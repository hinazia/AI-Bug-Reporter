name: Health Check

on:
    workflow_dispatch:
    schedule:
        - cron: '0 */12 * * *'

jobs:
    health-check-job:
        runs-on: ubuntu-latest
        steps:
            - name: Pull your latest Docker image
              run: 